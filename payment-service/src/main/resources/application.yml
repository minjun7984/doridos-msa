server:
  port: 8084

spring:
  application:
    name: payment-service
  profiles:
    active: local
  config:
    import: optional:configserver:http://localhost:8888

auth:
  token:
    secretKey: ${SECRET-KEY}
