server:
  port: 8082

spring:
  application:
    name: ticket-service
  profiles:
    active: prod
  config:
    import: optional:configserver:localhost:8888
auth:
  token:
    secretKey: ${SECRET-KEY}
